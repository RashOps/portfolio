export const metadata = {
  title: "Operator Profile",
  description: "Double cursus PSTB (Tech) & Excelia (Business). Profil hybride Data Scientist et Stratège Business.",
  alternates: { canonical: "/operator-profile" },
};

export default function Layout({ children }) {
  return <>{children}</>;
}
